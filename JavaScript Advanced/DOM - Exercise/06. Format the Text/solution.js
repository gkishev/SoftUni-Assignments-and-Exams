function solve() {
  const textArea = document.getElementById("input");
  const textAreaValue = textArea.value;
  const arrOfSentaces = textAreaValue.split(".");

  const result = document.getElementById("output");
}